{
  "name": "octopus-expertise",
  "version": "0.1.0",
  "private": true,
  "description": "OCTOPUS Expertise web platform monorepo",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev --workspace @octopus/web\" \"npm run start:dev --workspace @octopus/api\"",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run governance:test && npm run test --workspaces --if-present",
    "governance:test": "node --test tests/governance/*.test.mjs",
    "verify": "npm run format:check && npm run skills:check && npm run lint && npm test && npm run build",
    "format:check": "prettier --check apps docs README.md AGENTS.md package.json compose.yaml scripts/route-codex-skills.mjs tests/governance config .github",
    "skills:check": "node scripts/validate-codex-skills.mjs",
    "skills:route": "node scripts/route-codex-skills.mjs",
    "skills:route:test": "node --test tests/governance/skill-router.test.mjs",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2"
  }
}
